package conditions;

public enum EmployeeAttribute implements Attributes{
	id, address, birthDate, city, country, extension, firstname, hireDate, homePhone, lastname, photo, postalCode, region, salary, title, notes, photoPath, titleOfCourtesy
}
